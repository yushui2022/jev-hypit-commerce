import { sealVisualTrack } from '@hypit/hypit/composition';
import { browserProgram, hyperframesResourceUri } from '@hypit/hypit/hyperframes';
import { products } from './content.js';
export type Message={id:string;sender:string;text:string;side:string;at:any};
export type ChatOptions={id:string;title:string;entranceFrames:number};
export function renderChat(timeline:any,canvas:any,window:any,font:any,messages:Message[],options:ChatOptions,assets:Record<string,any>){
 let order=0;const children:any[]=[];
 const tx=(id:string,text:string,size=20,color='#edf6f6')=>{children.push({id,kind:'text',parent:'scene',order:++order,text,fonts:font.faces,style:[{name:'font-size',value:size+'px'},{name:'line-height',value:1.2},{name:'color',value:color},{name:'white-space',value:'nowrap'}]});return '{{'+id+'}}'};
 const urls=Object.fromEntries(Object.entries(assets).map(([k,v])=>[k,hyperframesResourceUri(v.resource)]));
 const face=(p:any)=>`background-image:url('${urls['sheet'+p.sheet]}');background-size:400% 500%;background-position:${(p.cell%4)*100/3}% ${Math.floor(p.cell/4)*25}%`;
 const selected=[3,39,46,82].map(id=>products.find(p=>p.index===id)!);
 const clips=selected.map((p,i)=>{
  const start=(18+i*3)*30,end=start+90,offset=[120,90,30,90][i]!;
  children.push({id:'vid'+i,kind:'video',parent:'scene',order:++order,artifact:assets['clip'+i],muted:true,style:[{name:'position',value:'absolute'},{name:'inset',value:0},{name:'width',value:'100%'},{name:'height',value:'100%'},{name:'object-fit',value:'cover'}],sampling:{sourceFrameRate:{numerator:30,denominator:1},sourceFrameCount:300,segments:[{target:{startFrame:0,endFrameExclusive:start},sourceFrame:{numerator:0,denominator:1},rate:{numerator:1,denominator:1},loop:{startFrame:0,endFrameExclusive:300}},{target:{startFrame:start,endFrameExclusive:end},sourceFrame:{numerator:offset,denominator:1},rate:{numerator:1,denominator:1}},{target:{startFrame:end,endFrameExclusive:1260},sourceFrame:{numerator:(offset+90)%300,denominator:1},rate:{numerator:1,denominator:1},loop:{startFrame:0,endFrameExclusive:300}}]}});
  return `<div class="clip" data-i="${i}">{{vid${i}}}<div class="clip-line"></div></div>`;
 }).join('');
 const html=`<div class="atmosphere"></div><div class="grid"></div><div class="particles">${Array.from({length:48},(_,i)=>`<i style="left:${(i*137)%1280}px;top:${(i*83)%720}px"></i>`).join('')}</div>
 <div class="orbital">${products.slice(0,24).map((p,i)=>`<div class="orbit-card" style="${face(p)}"></div>`).join('')}</div>
 <header><div class="brand">${tx('brand','贯一科技',20)}</div><div class="system">${tx('system','JEV × HYPIT',13,'#7d999c')}</div><div class="dot"></div></header>
 <section class="title"><div class="eyebrow">${tx('eyebrow','Jev × Hypit',23,'#58ebd7')}</div><div class="title-a">${tx('title-a','我们正在构建一个',31,'#a0b6b9')}</div><div class="title-b">${tx('title-b','AI Agent 原生的',61)}</div><div class="title-c">${tx('title-c','数字达人网络',82)}</div><div class="title-line"></div></section>
 <section class="thesis"><div>${tx('thesis-a','以商品匹配为起点',63)}</div><div class="thesis-line">${tx('thesis-b','让数字达人，走向内容创作与商业协作。',29,'#9ab7b8')}</div><div class="thesis-rule"></div></section>
 <section class="mechanism"><div class="mechanism-kicker">${tx('mechanism-kicker','从匹配决策，到内容呈现',28,'#c4d9da')}</div><div class="engine jev"><div class="engine-no">01 / DECISION</div><div>${tx('jev','Jev',66)}</div><div class="engine-title">${tx('jev-label','RLCD 校准决策',29,'#58ebd7')}</div><div class="engine-desc">${tx('jev-desc','结构化选择 · 置信度输出',22,'#b1c3c5')}</div><div class="engine-flow">${tx('jev-flow','商品特征 → 角色候选 → 匹配决策',19,'#859c9f')}</div><div class="engine-track"><i></i></div></div><div class="connection">→</div><div class="engine hypit"><div class="engine-no">02 / ORCHESTRATION</div><div>${tx('hypit','Hypit',66)}</div><div class="engine-title">${tx('hypit-label','语义时间线编排',29,'#a9b7ff')}</div><div class="engine-desc">${tx('hypit-desc','程序化动效 · 多轨合成',22,'#b1c3c5')}</div><div class="engine-flow">${tx('hypit-flow','素材与脚本 → 节奏编排 → 视频输出',19,'#859c9f')}</div><div class="engine-track"><i></i></div></div><div class="mechanism-note">${tx('note','视觉理解与 UGC 生成由媒体模型完成',16,'#829a9c')}</div></section>
 <section class="matching"><div class="product-main"><img></div><div class="match-path"><div></div><b>→</b></div><div class="avatar-main"></div><div class="match-rings"><i></i><i></i><i></i></div></section>
 <section class="ugc"><div class="ugc-product"><img></div><div class="ugc-line"><i></i></div><div class="ugc-shadow"></div></section>${clips}
 <section class="matrix"><div class="wall">${products.map((p,i)=>`<div class="tile" data-i="${i}"><div class="tile-face" style="${face(p)}"></div><div class="tile-product"><img src="${urls['product'+p.index]}"></div><div class="tile-glow"></div></div>`).join('')}</div></section>
 <section class="outro"><div class="outro-kicker">${tx('outro-kicker','AI AGENT–NATIVE CREATOR NETWORK',16,'#58ebd7')}</div><div class="outro-title">${tx('outro-title','Jev × Hypit',72)}</div><div class="outro-thesis">${tx('outro-thesis','从商品出发，让数字达人网络生长。',32,'#bbcfd0')}</div><div class="repo">${tx('repo','github.com/Yeadon8888/jev-hypit-commerce',26)}</div><div class="repo-rule"></div></section><div class="transition-light"></div>`;
 const css=`:scope{background:#080d12;overflow:hidden;font-family:sans-serif;color:#edf6f6}.atmosphere{position:absolute;inset:-20%;background:radial-gradient(ellipse at 30% 35%,#123d444a,transparent 45%),radial-gradient(ellipse at 76% 74%,#302b5844,transparent 50%)}.grid{position:absolute;inset:0;background-image:linear-gradient(#8fc9d20a 1px,transparent 1px),linear-gradient(90deg,#8fc9d20a 1px,transparent 1px);background-size:64px 64px;mask-image:radial-gradient(ellipse,black,transparent 80%)}header{position:absolute;left:44px;right:44px;top:29px;display:flex;align-items:center;gap:20px;z-index:30}.brand{letter-spacing:3px}.system{margin-left:auto;letter-spacing:2px}.dot{width:6px;height:6px;background:#58ebd7;border-radius:50%;box-shadow:0 0 16px #58ebd7}.particles{position:absolute;inset:0}.particles i{position:absolute;width:2px;height:2px;background:#8bbbbc;opacity:.25}.orbital{position:absolute;inset:0;perspective:1000px;opacity:.25}.orbit-card{position:absolute;left:580px;top:285px;width:100px;height:126px;border-radius:8px;border:1px solid #73dcc72e;backface-visibility:hidden}.title,.thesis,.mechanism,.outro{position:absolute;inset:0;text-align:center;z-index:8}.title .eyebrow{margin-top:148px;letter-spacing:4px}.title-a{margin-top:30px}.title-b{margin-top:19px}.title-c{margin-top:4px;letter-spacing:4px}.title-line{width:66px;height:3px;background:#58ebd7;margin:34px auto;box-shadow:0 0 30px #58ebd7}.thesis{padding-top:260px}.thesis-line{margin-top:30px}.thesis-rule{width:100px;height:2px;background:linear-gradient(90deg,#58ebd7,#9babff);margin:40px auto}.mechanism-kicker{position:absolute;top:125px;left:0;right:0}.engine{position:absolute;top:208px;width:473px;height:334px;border:1px solid #6be5d42b;border-radius:20px;background:linear-gradient(140deg,#122329ee,#0b121cee);text-align:left;padding:26px 28px;box-sizing:border-box}.engine.jev{left:112px}.engine.hypit{left:696px;border-color:#949ff82b}.engine-no{font-size:12px;color:#628b8e;letter-spacing:3px;margin-bottom:12px}.engine-title{margin-top:16px}.engine-desc{margin-top:14px}.engine-flow{margin-top:19px}.engine-track{position:absolute;bottom:0;left:0;right:0;height:2px;overflow:hidden}.engine-track i{display:block;width:120px;height:2px;background:#58ebd7;box-shadow:0 0 30px #58ebd7}.hypit .engine-track i{background:#a9b7ff}.connection{position:absolute;top:345px;left:610px;font-size:44px;color:#58ebd7}.mechanism-note{position:absolute;top:595px;left:0;right:0}.matching{position:absolute;inset:0;z-index:5}.product-main{position:absolute;left:180px;top:128px;width:410px;height:480px;border-radius:20px;overflow:hidden;background:#fff;box-shadow:0 20px 90px #0009}.product-main img{width:100%;height:100%;object-fit:contain;padding:18px;box-sizing:border-box}.avatar-main{position:absolute;left:733px;top:128px;width:367px;height:480px;border-radius:20px;box-shadow:0 20px 80px #0009;border:1px solid #5cf6dd55;background-repeat:no-repeat}.match-path{position:absolute;left:619px;top:359px;width:86px;height:2px;background:#58ebd744}.match-path div{position:absolute;top:-2px;width:28px;height:5px;background:#58ebd7;box-shadow:0 0 18px #58ebd7}.match-path b{position:absolute;right:-3px;top:-20px;font-size:34px;color:#58ebd7}.match-rings{position:absolute;left:676px;top:348px;opacity:.2}.match-rings i{position:absolute;width:28px;height:28px;border:1px solid #58ebd7;border-radius:50%}.ugc{position:absolute;inset:0;z-index:4}.ugc-product{position:absolute;left:158px;top:152px;width:420px;height:440px;border-radius:22px;background:#fff;overflow:hidden;box-shadow:0 20px 80px #000a}.ugc-product img{width:100%;height:100%;padding:20px;box-sizing:border-box;object-fit:contain}.ugc-line{position:absolute;left:592px;top:368px;width:93px;height:1px;background:#58ebd766}.ugc-line i{position:absolute;top:-3px;width:7px;height:7px;background:#58ebd7;border-radius:50%}.clip{position:absolute;overflow:hidden;isolation:isolate;border-radius:18px;border:1px solid #91ded655;background:#101a20;box-shadow:0 20px 80px #0009;z-index:10;will-change:transform}.clip-line{position:absolute;inset:0;border-radius:inherit;box-shadow:inset 0 0 0 1px #d8fffa22;pointer-events:none}.matrix{position:absolute;inset:0;perspective:1100px;overflow:hidden}.wall{position:absolute;left:640px;top:360px;width:0;height:0;transform-style:preserve-3d}.tile{position:absolute;width:116px;height:149px;left:-58px;top:-74px;border-radius:7px;background:#111d22;overflow:hidden;backface-visibility:hidden;box-shadow:0 5px 22px #000a;border:1px solid #6cfbe733;will-change:transform}.tile-face{position:absolute;inset:0;background-repeat:no-repeat}.tile-product{position:absolute;bottom:6px;right:6px;width:43px;height:47px;background:white;border-radius:5px;overflow:hidden;box-shadow:0 2px 10px #0006}.tile-product img{width:100%;height:100%;object-fit:contain}.tile-glow{position:absolute;inset:0;background:linear-gradient(135deg,#42f4d94a,transparent 65%);opacity:0}.outro{background:radial-gradient(ellipse at center,#0f2930 0%,#080d12 75%);z-index:20}.outro-kicker{margin-top:173px;letter-spacing:4px}.outro-title{margin-top:24px}.outro-thesis{margin-top:30px}.repo{margin-top:60px}.repo-rule{height:2px;width:280px;margin:25px auto;background:linear-gradient(90deg,transparent,#58ebd7,transparent)}.transition-light{position:absolute;inset:0;background:#91eee3;opacity:0;z-index:25;pointer-events:none}`;
 const setup=`
 const $=s=>root.querySelector(s),all=s=>Array.from(root.querySelectorAll(s));
 const title=$('.title'),thesis=$('.thesis'),mechanism=$('.mechanism'),match=$('.matching'),ugc=$('.ugc'),matrix=$('.matrix'),wall=$('.wall'),outro=$('.outro'),orbit=$('.orbital'),oc=all('.orbit-card'),tiles=all('.tile'),clips=all('.clip');
 const product=$('.product-main img'),avatar=$('.avatar-main'),up=$('.ugc-product img');
 const ease=x=>1-Math.pow(1-Math.max(0,Math.min(1,x)),3),clamp=x=>Math.max(0,Math.min(1,x));
 const show=(el,on)=>el.style.display=on?'block':'none';
 let last=-1,lastU=-1;
 const setFace=(el,p)=>{el.style.backgroundImage="url('"+data.urls['sheet'+p.sheet]+"')";el.style.backgroundSize='400% 500%';el.style.backgroundPosition=(p.cell%4)*100/3+'% '+Math.floor(p.cell/4)*25+'%';};
 return frame=>{
 const t=frame/30,beat=t*132/60,pulse=Math.pow(1-beat%1,5);
 show(title,t<4);show(thesis,t>=4&&t<7);show(mechanism,t>=7&&t<12);show(match,t>=12&&t<18);show(ugc,t>=18&&t<30);show(matrix,t>=30&&t<38);show(outro,t>=38);
 orbit.style.opacity=String(t<12?.15:t<30?.28:0);
 oc.forEach((el,i)=>{const a=i*Math.PI/12+t*.065,r=620+Math.sin(i*3)*60;el.style.transform='translate3d('+Math.cos(a)*r+'px,'+Math.sin(a)*290+'px,'+(-300+Math.cos(a)*150)+'px) rotateZ('+Math.sin(a)*12+'deg)';});
 title.style.opacity=String(ease(t/.65)*(1-clamp((t-3.6)/.4)));title.style.transform='translateY('+(1-ease(t/.9))*32+'px) scale('+(1+t*.007)+')';
 thesis.style.opacity=String(ease((t-4)/.5));thesis.style.transform='translateY('+(1-ease((t-4)/.7))*35+'px)';
 all('.engine').forEach((el,i)=>{const a=ease((t-7-i*.22)/.7);el.style.transform='translateY('+(1-a)*50+'px)';el.style.opacity=String(a);});
 all('.engine-track i').forEach((el,i)=>el.style.transform='translateX('+((t*160+i*220)%600-120)+'px)');
 if(t>=12&&t<18){const n=Math.floor((t-12)*2),idx=data.matchOrder[n%data.matchOrder.length],p=data.products[idx];if(last!==idx){product.src=data.urls['product'+p.index];setFace(avatar,p);last=idx;}const q=((t-12)*2)%1;match.style.transform='scale('+(1+.015*(1-ease(q*5)))+')';$('.match-path div').style.transform='translateX('+q*60+'px)';all('.match-rings i').forEach((el,i)=>{let a=(q+i/3)%1;el.style.transform='scale('+(1+a*6)+')';el.style.opacity=String(1-a);});}
 const active=Math.floor((t-18)/3);
 if(t>=18&&t<30&&lastU!==active){up.src=data.urls['product'+data.selected[active].index];lastU=active;}
 $('.ugc-line i').style.transform='translateX('+(t*90%90)+'px)';
 clips.forEach((el,i)=>{let on=(t>=18&&t<30&&active===i)||(t>=30&&t<32);show(el,on);if(t<30){const q=t-(18+i*3),a=ease(q/.4);el.style.left='724px';el.style.top='102px';el.style.width='304px';el.style.height='540px';el.style.transform='translateY('+(1-a)*30+'px) rotateZ('+(.4*Math.sin(q))+'deg)';el.style.opacity=String(a);}else{const a=ease((t-30)/.55);el.style.left=(54+i*307)+'px';el.style.top='120px';el.style.width='250px';el.style.height='445px';el.style.transform='translateY('+Math.sin(t*2+i)*10+'px) scale('+(1-.015*pulse)+')';el.style.opacity=String(1-clamp((t-31.7)/.3));}});
 if(t>=30&&t<38){const u=t-30,n=u<3.8?12:u<5.4?36:99,cols=n===12?4:n===36?9:17,rows=Math.ceil(n/cols),sc=n===12?1.35:n===36?.93:.52;wall.style.opacity=String(clamp((u-1.7)/.4));const ry=Math.sin(u*1.2)*13,rx=-7+Math.sin(u)*4;wall.style.transform='rotateX('+rx+'deg) rotateY('+ry+'deg) rotateZ('+Math.sin(u*.5)*2+'deg) scale('+(sc*(1+pulse*.025))+')';tiles.forEach((el,i)=>{show(el,i<n);const c=i%cols-(cols-1)/2,r=Math.floor(i/cols)-(rows-1)/2;const wave=Math.sin(c*.65+r*.8-u*9);let z=wave*35+Math.cos(c*.4+u)*25;el.style.transform='translate3d('+(c*126)+'px,'+(r*159)+'px,'+z+'px)';el.style.opacity=String(t>37.6?1-clamp((t-37.6)/.4):1);el.querySelector('.tile-glow').style.opacity=String(Math.max(0,wave)*.75);});}
 outro.style.opacity=String(ease((t-38)/.6));outro.style.transform='scale('+(1.035-.035*ease((t-38)/1))+')';
 const cuts=[4,7,12,18,21,24,27,30,32,38];$('.transition-light').style.opacity=String(Math.max(0,...cuts.map(c=>t>=c&&t<c+.12?.09*(1-(t-c)/.12):0)));
 };`;
 const matchOrder=[3,39,46,82,8,37,43,49,52,60,84,90].map(id=>products.findIndex(p=>p.index===id));
 const program=browserProgram({html,css,setup,data:{urls,products,selected,matchOrder}},Object.values(assets));
 return sealVisualTrack({id:options.id,programSpaceId:timeline.id,visualIr:'hypit.visual-ir@1',presents:[{id:options.id,span:window.span,stacking:{order:0,tieBreak:options.id},elements:[{id:'scene',kind:'program',order:0,program,style:[{name:'position',value:'absolute'},{name:'inset',value:0},{name:'width',value:canvas.widthPx+'px'},{name:'height',value:canvas.heightPx+'px'}]},...children]}]});
}
