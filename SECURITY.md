# Security

默认 localhost 单工作空间。API token 在启用时保护 `/api/*`，前端只在页面内存保存 token；不写 localStorage。公开 demo 媒体与前端不要求 token。图像读取限制在白名单目录并校验解析后的路径，业务生成文件通过任务 ID 和完成状态校验。

供应商密钥通过环境变量注入，不存储到 SQLite。任务错误不包含供应商响应体。发布前应检查代码、历史、媒体和配置中是否存在凭据。

当前不是多租户平台。需要对外生产部署时，增加 TLS、身份认证、限速、审计、资源配额和文件保留策略。不支持不可信用户共享同一服务器账户或修改白名单媒体目录。

发现凭据或安全问题请勿在公开 issue 贴出密钥。请通过 GitHub 仓库的可用私密报告渠道联系维护者。
