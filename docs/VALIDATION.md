# Validation · 2026-09-22

本次本地环境：macOS、Python 3.12、Node.js、Chrome、FFmpeg、Hypit 0.2.12。

| 验证 | 结果 / 范围 |
|---|---|
| pytest | 21 项通过：原 CLI 边界、并发幂等、互斥领取、租约过期、重试次数、过滤、审核快照、非法候选、供应商错误、API token、路径穿越与批量事务 |
| TypeScript | 全部 4 个 Hypit 组件编译通过 |
| Jev 真实请求 | 对商品 039 的 4 个召回候选执行 choice，返回合法 ID 和置信度 0.74；单次集成验证，不作为质量或速度 benchmark |
| 混合召回真实运行 | 下载 BGE small 英文模型，FastEmbed + 本地 Qdrant + RRF 对虚构 speaker 示例运行，tech 为第一候选 |
| 工作台端到端 | 浏览器提交商品 → 查看 8 个候选 → 人工批准 → Hypit 渲染 → 两种 MP4 完成 |
| 任务生成视频 | 两个水印版本均为 15 秒、1280×720、30fps |
| 新主片 | 两个水印版本均为 30 秒、1280×720、30fps，H.264 + AAC；抽帧检查开头、填格、UGC 与片尾 |
| GUI | 默认窄屏与 1440 宽屏检查，商品 / 候选图片正常，审核和渲染任务可操作 |
| 检索回归 | 10 条人工构造短查询，Precision@1 / Recall@3 / MRR 见 `evals/baseline-result.json` |

没有验证：Docker 完整部署、Linux 的最终视频渲染、远程 Qdrant、多租户、负载和长时间运行。Docker 配置用于进一步验证，不能据此宣称生产环境就绪。

检索回归集是小型词汇回归测试，含明显类别词，不代表真实商品长尾质量。100 个展示人物档案来自五组生成图集与编辑设定，不能作为达人经营数据或独立商业成功案例。
